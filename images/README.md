# Images

- `profile.jpg` — the sidebar portrait, wired up in the `author.avatar` field of
  `_config.yml`. The template crops it to a circle.
- `photos/` — 18 photographs, kept from the previous version of this site. Nothing
  displays them at the moment; the template has a `_portfolio` collection and a
  portfolio page layout if they should have a home again.
